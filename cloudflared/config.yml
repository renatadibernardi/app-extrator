# Cloudflare Tunnel config for this app-extrator repository only.
# Run with: cloudflared tunnel --config cloudflared/config.yml run app-extrator
ingress:
  - hostname: madrinhadosono.online
    path: /app-extrator.*
    service: http://localhost:3001
  - service: http_status:404
